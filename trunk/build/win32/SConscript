# import these variables from the parent build script
Import('env', 'sources', 'target_name')


env.Append(CPPFLAGS = "-mthreads");
env.Library(target=target_name, source=sources)

print "targetname: " + target_name

